include:
  - project: 'DevOps/templates'
    ref: 'v2.0.4'
    file:
      - '.compile/php.yml'
      - '.build/docker.yml'
      - '.deploy/kubectl.yml'

variables:
  JIRA_PROJECT_NAME: "PLAT"
  PRODUCTION_INCLUDES_STAGING: "true"

